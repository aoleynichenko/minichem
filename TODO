<> aoints to the separate module
<> output (like molden) to the separate module
<> one header in the /include dir = one module
<> direct scf & aoints-on-disk scf
<> module linalg => for LAPACK/BLAS wrappers & some other utilities

